{
  "name": "drip2hard",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@reach/router": "^1.2.1",
    "bulma": "^0.8.0",
    "firebase": "^7.3.0",
    "node-sass": "^4.13.0",
    "react": "^16.11.0",
    "react-bulma-components": "^3.1.3",
    "react-dom": "^16.11.0",
    "react-helmet": "^5.2.1",
    "react-scripts": "3.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "react-scripts build && firebase deploy",
    "functionsShell": "firebase functions:shell",
    "functions": "firebase serve --only functions",
    "localstart": "REACT_APP_LOCAL_FUNCTIONS=true react-scripts start",
    "ci_deploy": "react-scripts build && firebase deploy --token $FIREBASE_TOKEN --non-interactive"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
